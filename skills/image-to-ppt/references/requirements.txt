# Image-to-PPT Converter Dependencies / 图片转可编辑PPT 依赖
# ==============================================================
#
# Install / 安装方式：
#   pip install -r requirements.txt

# ─────────────────────────────────────────────────────────────
# Core pipeline / 核心管线 (image_to_ppt.py)
# ─────────────────────────────────────────────────────────────
# Background modeling, foreground extraction, PPTX assembly
# 背景建模、前景提取、PPTX 组装
python-pptx>=0.6.21
opencv-python>=4.5.0
Pillow>=9.0.0
numpy>=1.20.0
torch>=2.5.1
torchvision>=0.20.1
SAM-2 @ git+https://github.com/facebookresearch/sam2.git@main

# ─────────────────────────────────────────────────────────────
# OCR engines (at least one required for text detection)
# OCR 引擎（文本检测至少需要一个）
# ─────────────────────────────────────────────────────────────
#
# Option A: PaddleOCR (recommended for Chinese, better accuracy)
#   pip install paddleocr paddlepaddle
#
# Option B: Tesseract (lighter, requires system install)
#   pip install pytesseract
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   macOS: brew install tesseract
#   Ubuntu: sudo apt install tesseract-ocr tesseract-ocr-chi-sim
