# 网页版拆分提示词英文说明设计

## 目标

在 `Webpage__chunking_prompt.md` 末尾追加完整英文版，方便英文用户理解和直接使用该提示词。

## 修改范围

- 保留现有中文内容逐字不变。
- 在文件末尾追加分隔线和完整英文版。
- 英文版覆盖开头说明、转换效果示例标题以及 Prompt 1、Prompt 2。
- 不修改 `README.md` 或 `README_EN.md`。
- 按项目规则同步更新本地 `Course.md`，但不将其提交到 GitHub。

## 验证与发布

- 对比修改前后的中文内容，确认其逐字不变。
- 检查 Markdown 标题、列表和表格结构。
- 确认 Git 提交只包含设计规格和目标提示词，不包含 README 或本地 `Course.md`。
- 提交后推送到 `origin/main`。
