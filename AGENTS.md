## 5.项目文档维护

- 只要开发过程中新增、删除、修改了项目功能、页面、接口、路由、存储、任务流或运维行为，必须在同一轮同步更新项目根目录下的 `Course.md`。
- `Course.md` 必须保持简短，但要足够完整，能让下次接手时快速恢复上下文。
- `Course.md` 每次至少同步：当前项目状态、本轮新增或变更、关键修改文件、运行入口、当前注意事项。
- 每次同步完成后，自行判断是否存在描述滞后的旧信息，并删除这些对当前项目状态已经无用的内容。
- 不要等待用户额外提醒，把更新 `Course.md` 视为默认收尾步骤。

## 6.Git 提交规则

- 只要开发过程中对项目产生了新增、删除、修改了项目功能、页面、接口、路由、存储、任务流或运维行为，改动结束后顺便判断当前工作树是否适合提交 git。
- 如果当前工作树适合提交，应向用户提议是否提交新版本，并在得到用户明确确认后再执行 `git add` 和 `git commit`(git commit命名用简体中文)。
- 如果当前工作树不适合提交，需要向用户说明不适合提交原因；若用户没有明确确认提交，代理应保留改动为未提交状态。
- `git push` 仍需用户单独明确授权。